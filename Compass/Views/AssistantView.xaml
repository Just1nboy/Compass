<UserControl x:Class="Compass.Views.AssistantView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             FontFamily="Segoe UI">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="30,26,30,30" MaxWidth="860" HorizontalAlignment="Left">

            <TextBlock Text="✨  What am I missing?" Foreground="{StaticResource Text}" FontSize="24" FontWeight="Bold"/>
            <TextBlock TextWrapping="Wrap" Foreground="{StaticResource Muted}" FontSize="14" Margin="0,3,0,16"
                       Text="Describe any situation in your own words. The app will tell you the steps to take, the exact questions you should ask, and where to find the answers — so you stop getting caught out by things you didn't know to ask about."/>

            <TextBlock Text="Try one of these:" Foreground="{StaticResource Muted}" FontSize="12" Margin="0,0,0,6"/>
            <WrapPanel x:Name="Examples" Margin="0,0,0,12"/>

            <TextBox x:Name="SituationBox" FontSize="14" Padding="12,10" MinHeight="90"
                     TextWrapping="Wrap" AcceptsReturn="True" VerticalContentAlignment="Top"/>

            <StackPanel Orientation="Horizontal" Margin="0,12,0,0">
                <Button x:Name="GenerateBtn" Content="Generate my checklist" Style="{StaticResource Primary}" Click="Generate_Click"/>
                <TextBlock x:Name="Status" Foreground="{StaticResource Muted}" FontSize="13" VerticalAlignment="Center" Margin="14,0,0,0"/>
            </StackPanel>

            <Border x:Name="ResultCard" Visibility="Collapsed" Background="{StaticResource Surface}" BorderBrush="{StaticResource Line}"
                    BorderThickness="1" CornerRadius="12" Padding="18,16" Margin="0,20,0,0">
                <StackPanel>
                    <TextBlock x:Name="ResultTitle" Foreground="{StaticResource Text}" FontSize="18" FontWeight="Bold" TextWrapping="Wrap"/>
                    <TextBlock x:Name="ResultDesc" Foreground="{StaticResource Muted}" FontSize="13" TextWrapping="Wrap" Margin="0,3,0,10"/>
                    <StackPanel x:Name="ResultSteps"/>
                    <Button x:Name="SaveBtn" Content="＋  Save to my Playbooks" Style="{StaticResource Primary}"
                            HorizontalAlignment="Left" Margin="0,14,0,0" Click="Save_Click"/>
                </StackPanel>
            </Border>

        </StackPanel>
    </ScrollViewer>
</UserControl>
